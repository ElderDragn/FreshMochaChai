// Generally you're going to want to test code you've written from other files. This is that file.


const skyIsBlue = true;

var myTaxes = 'Still not done';

function doTaxes() {
  myTaxes = 'Done!';
}

function square(x) {
  return x * x;
}